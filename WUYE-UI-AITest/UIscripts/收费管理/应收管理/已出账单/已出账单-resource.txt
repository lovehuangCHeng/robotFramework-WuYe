*** Settings ***
Variables         ../../../../config/elements/收费管理/应收Xpath.py
Library           Selenium2Library    timeout=20
Resource          ../../../通用方法.robot
Library           string

*** Keywords ***
高级搜索（已出）
    [Arguments]    ${SD}    ${CD}
    Click button    ${高级搜索下拉框}
    sleep    2
    input text    ${应收日期}    ${SD}
    sleep    2
    click button    ${应收日期确定按钮}
    sleep    1
    input text    ${房间代码 }    ${CD}
    sleep    1
    Click Element    ${账单状态 }
    sleep    1
    click element    ${选择已出账单}
    sleep    1
    click button    ${单击搜索操作}

导出excel
    sleep    5
    Click button    ${导出excel}
    sleep    1

导出明细excel
    sleep    5
    Click button    ${导出明细excel}
    sleep    1

删除没有收费的账单
    sleep    3
    Click Element    ${勾选外层账单}
    sleep    1
    Click button    ${删除账单}
    sleep    2
    Click element    ${已出二次确认}
    等待element元素可见    ${关闭撤销删除页面}
    sleep    2
    Click button    ${关闭撤销删除页面}

删除账单并撤销收费(回收票据)
    sleep    3
    Click Element    ${勾选外层账单}
    sleep    1
    Click button    ${删除账单}
    sleep    2
    Click element    ${已出二次确认}
    sleep    2
    Click Element    ${选择回收票据}
    sleep    2
    Click Element    ${勾选账单}
    sleep    1
    Click button    ${撤销并删除账单}
    等待button元素可见    ${确认撤销账单}
    sleep    2
    Click button    ${确认撤销账单}

删除账单并撤销收费(不回收票据)
    Click Element    ${勾选外层账单}
    sleep    1
    Click button    ${删除账单}
    sleep    2
    Click element    ${已出二次确认}
    sleep    2
    Click Element    ${勾选账单}
    sleep    1
    Click button    ${撤销并删除账单}
    等待button元素可见    ${确认撤销账单}
    sleep    2
    Click button    ${确认撤销账单}

删除已扎帐的账单
    sleep    3
    Click Element    ${勾选外层账单}
    sleep    1
    Click button    ${删除账单}
    sleep    2
    Click element    ${二次确认}
    sleep    2
    Click button    ${关闭撤销删除页面}

冻结账单
    sleep    5
    Click Element    ${勾选外层账单}
    sleep    2
    Click button    ${冻结账单}
    sleep    2
    Click element    ${已出二次确认}

解冻账单
    Click button    ${高级搜索下拉框}
    sleep    2
    Click Element    ${账单状态 }
    sleep    2
    等待element元素可见    ${选择冻结账单 }
    click element    ${选择冻结账单 }
    sleep    1
    click button    ${单击搜索操作}
    sleep    2
    Click Element    ${勾选外层账单}
    sleep    2
    Click button    ${解冻账单}
    sleep    2
    Click element    ${已出二次确认}

标记开票
    Click Element    ${勾选外层账单}
    sleep    1
    Click button    ${标记为开票}
    sleep    2
    Click element    ${已出二次确认}

查询获取未缴费已出账单
    ${NBCode}    Set Variable    ${var[0][0]}
    ${NBSDay}    Set Variable    ${var[0][1]}
    Set global Variable    ${NBCode}
    Set global Variable    ${NBSDay}
    LOG    ${NBCode}
    LOG    ${NBSDay}
    sleep    1
    高级搜索（已出）    '${NBSday}'    ${NBCode}

查询获取部分缴费且打印已出账单
    ${NBCode1}    Set Variable    ${var[0][0]}
    ${NBSDay1}    Set Variable    ${var[0][1]}
    Set global Variable    ${NBCode1}
    Set global Variable    ${NBSDay1}
    LOG    ${NBCode1}
    LOG    ${NBSDay1}
    sleep    1
    高级搜索（已出）    '${NBSday1}'    ${NBCode1}

查询获取缴费且未打印票据
    ${NBCode2}    Set Variable    ${var[0][0]}
    ${NBSDay2}    Set Variable    ${var[0][1]}
    Set global Variable    ${NBCode2}
    Set global Variable    ${NBSDay2}
    LOG    ${NBCode2}
    LOG    ${NBSDay2}
    高级搜索（已出）    '${NBSday2}'    ${NBCode2}

部分缴费且未打印票据且(已扎帐)
    ${NBCode3}    Set Variable    ${var[0][0]}
    ${NBSDay3}    Set Variable    ${var[0][1]}
    Set global Variable    ${NBCode3}
    Set global Variable    ${NBSDay3}
    LOG    ${NBCode3}
    LOG    ${NBSDay3}
    高级搜索（已出）    '${NBSday3}'    ${NBCode3}
